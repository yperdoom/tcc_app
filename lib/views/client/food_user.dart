import 'package:flutter/material.dart';

class FoodUser extends StatelessWidget {
  const FoodUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('You are in Food page')));
  }
}
